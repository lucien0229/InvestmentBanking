# Semantic change impact proposal

Inspect one material change and its deterministic typed dependency closure. Propose semantic consequences only for exact candidate objects already included in the supplied closure.

AI output is proposal-only. It cannot create, remove, overwrite, resolve, invalidate, authorize, or mark readiness for any Impact item, Revision, Decision, QC result, package, or external-use decision. Preserve every supplied identifier verbatim. If the closure is incomplete or the object cannot be tied to an exact candidate, abstain with the smallest recovery action.
