# 03 — Turn a qualified buyer into a reconciled Individual entitlement

**What to build:** Connect Outcome, Pricing, Qualification, Account Access, Terms, Stripe Checkout, durable canonical provider evidence, Product Entitlement, Account plan, capacity, and receipt into one buyer-visible purchase loop. Payment remains evidence; only product-owned reconciliation grants the named Individual Banker exactly two Active Deal slots and the complete V1 core capability.

**Blocked by:** 01 — Establish the Supabase-authenticated Reference Deal acceptance seam.

**Status:** resolved

- [x] Pricing and confirmation surfaces show $995 monthly, $10,950 annual, two Active Deals, the confirmed per-Deal allowances, add-ons, unmetered actions, renewal, cancellation, guarantee, and tax posture/amount before payment confirmation.
- [x] Qualification and Account Access can be resumed safely and do not create entitlement before product-owned provider reconciliation succeeds.
- [x] Browser returns, duplicate/reordered Webhooks, provider retries, and repeated product commands produce one charge record, one entitlement mutation, one checkout-completed event, and one capacity allocation.
- [x] Every processable provider event durably records its versioned canonical evidence and digest before acknowledgment; raw Webhook bytes are used only ephemerally for signature verification, are not persisted, and are not required for recovery while INT-DEF-003 remains unresolved.
- [x] Successful purchase creates one named Individual entitlement with exactly two available Active Deal slots and complete core capability; provider status alone cannot grant Account, Deal, Recipient, or Human Decision authority.
- [x] Receipt, plan, entitlement, and capacity reads agree across Account UI, API, authoritative state, Audit, and privacy-safe measurement candidates.
- [x] Black-box fixtures cover monthly/annual, duplicate, out-of-order, ambiguous, invalid-signature, persistence-failure, conflicting-digest, and repeated-command paths and satisfy AC-009 through AC-011 and AC-069.

## Comments

2026-08-27 — Ticket 03 implementation completed at the local/test-adapter boundary. The vertical slice adds the public offer and non-confidential qualification/recovery API, durable Order → Terms → Payment → Confirmation routes with `If-Match` and idempotency, canonical Stripe evidence plus digest and provider-event outbox records, product-owned reconciliation, one receipt/entitlement/mutation/completed event/capacity grant, Account commerce projections, privacy-safe Audit and measurement candidates, and the Capability Manifest surface. The browser Order flow now selects an add-on, shows its exact preview/amount/effect, and requires explicit add-on consent before Terms; Terms carries the same acknowledgement; Usage and Confirmation render the authoritative add-on/capacity projection. Commerce SECURITY DEFINER procedures now have a dedicated `app_commerce_owner` `NOLOGIN` owner with PUBLIC execution revoked, and validly signed malformed Webhook input fails with `400 invalid_request`.

Primary files: `apps/api/src/commerce.ts`, `apps/api/src/app.ts`, `apps/api/src/database.ts`, `db/migrations/0003_commerce_entitlement.sql`, `apps/web/app/pricing/page.tsx`, `apps/web/app/qualification/page.tsx`, `apps/web/app/checkout/order/page.tsx`, `apps/web/app/checkout/terms/page.tsx`, `apps/web/app/checkout/payment/page.tsx`, `apps/web/app/checkout/confirmation/page.tsx`, `apps/web/app/app/account/usage-plan/page.tsx`, `apps/web/app/capability-manifest/page.tsx`, `contracts/openapi.json`, and `tests/commerce.test.ts`.

The session command holds a transaction-scoped advisory lock keyed by Account and Idempotency-Key while creating the provider session, then persists the key/request digest with the product order. Terms acceptances likewise require and persist an Idempotency-Key.

Verification on fresh isolated PostgreSQL database `investment_banking_ticket03i` (`127.0.0.1:55432`): `npm test` passed 15/15; `npx tsc --noEmit` passed; `npm run web:build` passed; `npm run contracts:check` passed; `git diff --check` passed. The commerce suite asserts exact $995 monthly / $10,950 annual amounts, annual success, 2-slot baseline entitlement and complete V1 capability, explicit add-on capacity effects, duplicate/reordered/conflicting provider events, unpaid and ambiguous fail-closed paths, invalid signatures, evidence-persistence failure, no raw Webhook payload column, repeated terms/order/session Idempotency-Key behavior, and qualification restoration without Source content.

Provider boundary: the default local/test configuration uses the isolated `stripe_test_adapter` and signed contract fixtures; responses expose `live_verification_debt: true`. No mock payment is treated as a real charge. Production startup rejects the test adapter and missing production Webhook secret. Live Stripe Checkout, live Webhook delivery/retry behavior, live tax calculation/invoice/refund behavior, production Supabase/provider configuration, and final browser/human sign-off remain unverified; the ticket is resolved for the authorized development environment, not production-complete.

Code-review follow-up: the local dispatcher reconciles a durable Outbox row in the Webhook request, but a separately scheduled consumer/replay worker and provider current-object lookup are still live-integration hardening work. A provider accepting a hosted Session immediately before an application crash can still require provider-side idempotency/current-object recovery. The deployment must set `APP_ENV=production`, a live Checkout adapter, and a production Webhook secret; local tax remains zero until the live tax/invoice seam is verified. No Ticket 02/04 files were changed.

Known deferred integration debt: canonical evidence intentionally excludes raw Webhook bytes while INT-DEF-003 is unresolved (a raw-payload digest is retained, but raw replay is not available); the local dispatcher marks the typed Outbox row after reconciliation, while separately scheduled worker recovery and current-provider-object lookup still require the live integration hardening gate. The local tax posture is explicit but uses a zero test amount until a live tax/invoice adapter supplies a jurisdictional calculation. These are recorded as live/deferred verification risks, not claims that the test adapter collected money.

Scope boundary: only Ticket 03 files and its direct API/contract/RLS test surfaces were changed. Ticket 02, Ticket 04, and later ticket files were not modified. At implementation verification the worktree was uncommitted; no push, PR, or cleanup was performed.

## Answer

2026-08-31 — Resolved for the authorized development environment. The Ticket 02 development release was preserved as the base and the Ticket 03 vertical slice was deployed as `/opt/investmentbanking/releases/20260831-ticket03-stripe`; the `current` symlink points to that release. Supabase Dev project `xuysyaxzcpntvvzsgkdy` applied migration `commerce_entitlement_v1`, exposing the commerce tables/functions with forced RLS. Supabase hosted Dev rejected only the local-only `ALTER ROLE app_commerce_owner` clause, so the Dev migration used the compatible variant that retains the closed SECURITY DEFINER procedures and app-runtime grants while keeping the repository migration's dedicated offline owner posture for local/test evidence.

Development verification: local merged Ticket 02 + Ticket 03 assembly passed `npm test` (19/19), `npm run build`, and `npm run contracts:check`. On the server, API/Web/Nginx are active and running; `/`, `/pricing`, `/qualification`, `/account-access`, `/checkout/order`, `/checkout/terms`, `/checkout/payment`, `/checkout/confirmation`, `/capability-manifest`, and `/project-northstar` return 200; `/api/v1/public/offer` returns the exact V1 commercial contract; qualification returns 201 with `source_material_authorized=false`; protected checkout without a session returns 401; `/webhooks/stripe` is publicly routed and rejects missing/invalid signatures with 400.

Stripe Test Mode is now wired through the real `StripeCheckoutAdapter`: Products/Prices for monthly `$995`, annual `$10,950`, the V1 add-ons, and the endpoint `https://dev-banking.aptoren.com/webhooks/stripe` were created in the connected Stripe sandbox. The endpoint is enabled only for `checkout.session.completed`; the endpoint-specific signing secret and six Price IDs are present in the remote Dev environment, and both API/Web services are active. Managed Payments is explicitly disabled for this development flow because tax configuration was deferred by authorization.

Live sandbox evidence: Stripe Checkout Session `cs_test_a1MLbFlnxMcQTpcU4Cm69pochtcJPTmbpIsyZbZW2f1UbniWJzBn9Jqgfq` was completed with the Stripe CLI `payment_page` test fixture (`$995` monthly), producing event `evt_1UAQF32SUlTvcsKtHj1cMtYG`. After the nullable subscription-field evidence fix, a signed delivery returned `200 reconciled`; a repeated delivery returned `200 duplicate`. The authoritative Dev order is `succeeded/completed` with one paid receipt, one named Individual entitlement, exactly two Active Deal slots, and the complete V1 core capability. Supabase counts for that order are `receipts=1`, `mutations=1`, `completed_events=1`, `grants=1`, `provider_events=1`, `outbox=1`; Account commerce state reports the same receipt/entitlement/capacity and one Audit plus one privacy-safe measurement candidate.

The remaining items are explicit risks rather than hidden blockers: tax calculation/invoice/refund/renewal behavior is not configured or verified; this is Stripe Test Mode and not evidence of production collection; the independent provider-outbox worker/current-provider-object lookup/replay hardening remains deferred (INT-DEF-003); and browser UI automation was not independently walked because the in-app Stripe tab automation timed out—the hosted Checkout and webhook path were verified through Stripe API/CLI plus signed HTTP delivery. Supabase advisor INFO/WARN findings remain non-blocking development hardening. No Ticket 02/04 files were changed, and no push, PR, or worktree cleanup was performed.

2026-08-31 code-review follow-up (read-only): the development configuration is resolved, but the review identified implementation hardening outside the configured sandbox path. Qualification is advisory and checkout can be opened directly without binding an assessment; failure/recovery/invoice/cancellation lifecycle states are not implemented beyond `checkout.session.completed`; a truly out-of-order event can remain `ambiguous` until the deferred worker/current-provider-object recovery exists; and `GET /api/v1/checkout-sessions/{id}` does not currently project provider-boundary fields (`provider`, `live_verification_debt`, `hosted_url`). Standards review also flagged the synchronous Outbox dispatch, hand-rolled signature verification, missing raw-body limit, cache/ETag/Location/header conformance, and ad-hoc digest canonicalization. These findings are recorded as follow-up engineering debt and were not expanded into Ticket 02/04 or unrelated scope.

### Integration update — 2026-08-31

The Ticket 03 implementation commit was integrated into the current `main` as `c916438` after resolving the intentional overlap with the already-integrated Ticket 02 public proof in `apps/api/src/app.ts`, the home page, pricing, qualification, and generated API surfaces. Ticket 02 behavior remains reachable, and the Ticket 03 checkout/entitlement routes are now on the same mainline. This tracker update and the feature map are being synchronized separately; no Ticket 04 or later-ticket scope was advanced.
